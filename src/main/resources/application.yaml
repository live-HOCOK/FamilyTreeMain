spring:
  application:
    name: 'FamilyTreeMain'
  config:
    import: classpath:db_postgres.yaml
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: "veryVeryVeryVeryVeryVerySecretKey123"
  expiration: 86400000

logging:
  level:
    root: WARN
    com.kitokot.familytree: TRACE
  pattern:
    level: "%5p [%X{requestId}]"