spring:
  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://localhost:5432/family_tree}
    username: ${DATASOURCE_USERNAME:postgres}
    password: ${DATASOURCE_PASSWORD:postgres}
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect